import Link from 'next/link';
import { notFound } from 'next/navigation';
import { richiediContesto } from '@/server/identity/contesto';
import { dettaglioComposizioneListino, elencoComponentiPerComposizione } from '@/server/services/listino-composizioni-service';
import { listaBomAdmin } from '@/server/services/bom-admin-service';
import { ComposizioneBomForm } from '@/app/admin/catalogo/listino/composizione-bom-form';
import { aggiungiRigaComposizioneAzione, aggiornaRigaComposizioneAzione, rimuoviRigaComposizioneAzione } from '@/app/admin/composizioni-azioni';
export const dynamic='force-dynamic';
const euro=new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR'});
export default async function ComposizioneListinoPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const cat=await richiediContesto({modulo:'catalogo',azione:'leggi'});const req=await richiediContesto({modulo:'richieste',azione:'leggi'});const dettaglio=await dettaglioComposizioneListino(cat.tenantId,id);if(!dettaglio)notFound();const [componenti,bom]=await Promise.all([elencoComponentiPerComposizione(cat.tenantId,id),listaBomAdmin(req.tenantId)]);const bomBozza=bom.filter(b=>b.stato==='BOZZA');return <main className="mx-auto max-w-7xl px-6 py-12"><Link href="/admin/catalogo/listino" className="text-sm text-muted-foreground hover:underline">← Listino Atelier</Link><div className="mb-8 mt-2 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-muted-foreground">{dettaglio.composizione.codice} · Composizione</p><h1 className="text-3xl font-semibold">{dettaglio.composizione.nome}</h1><p className="mt-2 text-sm text-muted-foreground">{[dettaglio.composizione.larghezzaCm,dettaglio.composizione.altezzaCm,dettaglio.composizione.profonditaCm].some(v=>v!=null)?`${[dettaglio.composizione.larghezzaCm,dettaglio.composizione.altezzaCm,dettaglio.composizione.profonditaCm].map(v=>v??'—').join(' × ')} cm`:''} · {dettaglio.composizione.materiale??'Materiale non specificato'}</p></div><div className="rounded-lg border px-5 py-4 text-right"><div className="text-xs uppercase tracking-wide text-muted-foreground">Prezzo listino</div><div className="text-2xl font-semibold">{euro.format(dettaglio.composizione.prezzo)}</div></div></div><section className="mb-6 grid gap-4 sm:grid-cols-3"><div className="rounded-lg border p-4"><div className="text-xs uppercase tracking-wide text-muted-foreground">Costo distinta</div><div className="mt-1 text-2xl font-semibold">{euro.format(dettaglio.costoDistinta)}</div></div><div className="rounded-lg border p-4"><div className="text-xs uppercase tracking-wide text-muted-foreground">Voci in distinta</div><div className="mt-1 text-2xl font-semibold">{dettaglio.righe.length}</div></div><div className="rounded-lg border p-4"><div className="text-xs uppercase tracking-wide text-muted-foreground">Differenza di riferimento</div><div className="mt-1 text-2xl font-semibold">{euro.format(dettaglio.composizione.prezzo-dettaglio.costoDistinta)}</div></div></section><section className="mb-6 rounded-lg border p-5"><h2 className="font-semibold">Aggiungi componente alla composizione</h2><p className="mt-1 text-sm text-muted-foreground">Il costo corrente viene copiato come snapshot.</p><form action={aggiungiRigaComposizioneAzione} className="mt-4 grid gap-3 md:grid-cols-[1fr_160px_auto]"><input type="hidden" name="composizioneId" value={id}/><select name="componenteId" required className="rounded-md border bg-background p-2"><option value="">Seleziona materiale o componente…</option>{componenti.map(c=><option key={c.id} value={c.id}>{c.nome} · {c.unita} · {euro.format(c.prezzo)}</option>)}</select><input name="quantita" type="number" min="0.001" step="0.001" defaultValue="1" required className="rounded-md border p-2"/><button className="rounded-md bg-primary px-4 py-2 text-sm text-primary-foreground">Aggiungi</button></form></section><section className="mb-6 rounded-lg border p-5"><h2 className="font-semibold">Trasferisci la composizione in una BOM</h2><p className="mt-1 text-sm text-muted-foreground">Le righe vengono copiate nella BOM come snapshot. La BOM deve essere BOZZA.</p>{bomBozza.length===0?<p className="mt-4 text-sm text-muted-foreground">Nessuna BOM in bozza disponibile.</p>:dettaglio.righe.length===0?<p className="mt-4 text-sm text-muted-foreground">Aggiungi almeno un componente prima del trasferimento.</p>:<div className="mt-4"><ComposizioneBomForm composizioneId={id} bomBozza={bomBozza}/></div>}</section><section className="overflow-x-auto rounded-lg border"><table className="w-full min-w-[900px] text-sm"><thead className="bg-secondary/40 text-left text-muted-foreground"><tr><th className="p-3">Tipo</th><th className="p-3">Componente</th><th className="p-3">Unità</th><th className="p-3">Quantità</th><th className="p-3 text-right">Costo unitario</th><th className="p-3 text-right">Costo totale</th><th className="p-3">Azione</th></tr></thead><tbody className="divide-y">{dettaglio.righe.map(r=><tr key={r.id}><td className="p-3">{r.tipo==='MATERIALE'?'Materiale':'Componente'}</td><td className="p-3"><div className="font-medium">{r.nome}</div><div className="text-xs text-muted-foreground">{r.codice}</div></td><td className="p-3">{r.unita}</td><td className="p-3"><form action={aggiornaRigaComposizioneAzione} className="flex gap-2"><input type="hidden" name="id" value={r.id}/><input type="hidden" name="composizioneId" value={id}/><input name="quantita" type="number" min="0.001" step="0.001" defaultValue={r.quantita} className="w-24 rounded-md border p-2"/><button className="rounded-md border px-3 py-2">Salva</button></form></td><td className="p-3 text-right">{euro.format(r.costoUnitario)}</td><td className="p-3 text-right font-semibold">{euro.format(r.costoTotale)}</td><td className="p-3"><form action={rimuoviRigaComposizioneAzione}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="composizioneId" value={id}/><button className="rounded-md border px-3 py-2">Rimuovi</button></form></td></tr>)}{dettaglio.righe.length===0&&<tr><td colSpan={7} className="p-10 text-center text-muted-foreground">Nessun componente inserito.</td></tr>}</tbody></table></section></main>}
