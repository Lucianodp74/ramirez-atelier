# Dettaglio composizione

La rotta `/admin/catalogo/listino/[id]` è disponibile per le voci `COMPOSIZIONE` e permette di costruire la distinta interna della composizione con materiali e componenti del Listino Atelier.
